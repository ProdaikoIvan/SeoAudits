(function() {
    'use strict';

    angular
        .module('app')
        .controller('IndexStatusCachedSnapshotCtrl', IndexStatusCachedSnapshotCtrl);

    IndexStatusCachedSnapshotCtrl.$inject = ['$scope'];
    function IndexStatusCachedSnapshotCtrl($scope) {
        this.$onInit = function() {
            var vm = this;
        };

    }
})();